#include <stdio.h>
#include <stdlib.h>

void  o()
{
  system("/bin/sh");
  exit (1);
}

int n()
{
  char *str

  fgets(str, 512, stdin);
  printf(str);
  exit (1);
}

int main()
{
  n();
  return (0);
}
