#include <stdio.h>

unsigned int m;

void p(char *s)
{
  printf(s);
  return;
}

void n()
{
  char *str;

  fgets(str, 512, stdin);
  p(str);
  if (m == 16930116) {
    system("/bin/cat /home/user/level5/.pass");
  }

  return;
}

int main()
{
  n();
  return 0;
}